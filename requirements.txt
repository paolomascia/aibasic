# Core dependencies for AIBasic compiler
requests>=2.31.0

# Optional dependencies for modules
# Uncomment the ones you need:

# PostgreSQL module
psycopg2-binary>=2.9.9

# MySQL/MariaDB module
mysql-connector-python>=8.0.33

# RabbitMQ module
pika>=1.3.2

# Apache Kafka module
kafka-python>=2.0.2

# Redis module
redis>=5.0.0

# OpenSearch module
# opensearch-py>=2.0.0
# For AWS IAM auth (optional):
boto3>=1.26.0
requests-aws4auth>=1.1.0

# Compression module (most formats built-in, 7Z optional)
py7zr>=0.20.0  # For 7Z format support

# HashiCorp Vault module
hvac>=1.2.0  # HashiCorp Vault client

# Apache Cassandra module
cassandra-driver>=3.28.0  # DataStax Python driver for Cassandra

# MongoDB module
pymongo>=4.0.0  # MongoDB Python driver

# S3/MinIO module
boto3>=1.26.0  # AWS SDK for Python (S3, MinIO, DigitalOcean Spaces, etc.)

# SSH module
paramiko>=3.0.0  # SSH2 protocol library for remote server connections

# Microsoft Teams module
msal>=1.20.0  # Microsoft Authentication Library for Python (for Graph API)
requests>=2.31.0 already included above

# Slack module
requests>=2.31.0 already included above
urllib3 included with requests

# ClickHouse module
# requests>=2.31.0 already included above
pandas>=1.5.0  # For DataFrame operations (if not already installed)

# Neo4j module
neo4j>=5.0.0  # Neo4j Python driver for graph database

# Elasticsearch module
elasticsearch>=8.0.0  # Elasticsearch client for full-text search and analytics

# TimescaleDB module (uses PostgreSQL driver)
# psycopg2-binary>=2.9.9 already included above
